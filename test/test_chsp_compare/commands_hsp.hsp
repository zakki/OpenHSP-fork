#include "hsp3cl.as"

#module

#deffunc fill_vals array dst, local v
	v = 10
	dst(0) = v
	v += 5
	dst(1) = v
	v -= 2
	dst(2) = v
	v *= 3
	dst(3) = v
	return
#global

	dim vals, 4
	fill_vals vals

	mes "COMMANDS"
	repeat 4
		mes "" + vals(cnt)
	loop
	end
