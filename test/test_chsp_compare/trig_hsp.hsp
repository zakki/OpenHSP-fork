#include "hsp3cl.as"

#module

#deffunc fill_trig array vals, double angle
	vals(0) = sin(angle)
	vals(1) = cos(angle)
	vals(2) = tan(angle * 0.25)
	vals(3) = atan(1.0)
	return
#global

	ddim vals, 4
	fill_trig vals, 0.5

	mes "TRIG"
	repeat 4
		mes strf("%.6f", vals(cnt))
	loop
	end
