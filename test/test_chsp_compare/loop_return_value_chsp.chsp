#include "hsp3cl.as"

#chsp_module "loop_return_value_chsp"

#chsp_defcfunc int first_over int limit
repeat 6
	if cnt > limit {
		return cnt * 10 + 1
	}
loop
return 999
#chsp_end

#chsp_module_end

mes "" + first_over(0)
mes "" + first_over(5)
mes "" + first_over(1)
mes "" + first_over(10)

end
